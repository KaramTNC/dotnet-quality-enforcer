name: CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: python -m pip install --upgrade pip && python -m pip install -c constraints-ci.txt -e ".[dev]"

      - name: Run unit tests
        run: python -m unittest discover -s tests -p "test_*.py"

      - name: Compile package
        run: python -m compileall -q src

      - name: Verify command-line entry point
        run: dotnet-quality --help

  static-analysis:
    name: Ruff and mypy
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install development dependencies
        run: python -m pip install --upgrade pip && python -m pip install -c constraints-ci.txt -e ".[dev]"

      - name: Run Ruff
        run: ruff check src tests action_runner.py

      - name: Run mypy
        run: mypy src action_runner.py

  windows:
    name: Windows compatibility
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.x"

      - name: Install package
        run: python -m pip install --upgrade pip; python -m pip install -c constraints-ci.txt -e ".[dev]"

      - name: Run unit tests
        run: python -m unittest discover -s tests -p "test_*.py"

      - name: Build Roslyn helper
        run: dotnet build tools/roslyn-analyzer/DotnetQualityRoslyn.csproj --configuration Release --nologo

      - name: Verify Windows CLI and JSON contract
        run: dotnet-quality --output json code-size --scope full

  security:
    name: Dependency audit
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install package and audit tooling
        run: python -m pip install --upgrade pip && python -m pip install -c constraints-ci.txt -e ".[security]"

      - name: Audit installed dependencies
        run: pip-audit .

  roslyn-helper:
    name: Build Roslyn helper
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.x"

      - name: Build Roslyn helper
        run: dotnet build tools/roslyn-analyzer/DotnetQualityRoslyn.csproj --configuration Release --nologo

      - name: Smoke test Roslyn helper
        run: |
          dotnet tools/roslyn-analyzer/bin/Release/net8.0/DotnetQualityRoslyn.dll \
            --file tools/roslyn-analyzer/Program.cs > roslyn-output.json
          python -c "import json; payload=json.load(open('roslyn-output.json')); assert 'types' in payload and 'diagnostics' in payload"

      - name: Smoke test Roslyn batch protocol
        run: |
          dotnet tools/roslyn-analyzer/bin/Release/net8.0/DotnetQualityRoslyn.dll \
            --files tools/roslyn-analyzer/Program.cs tools/roslyn-analyzer/Program.cs > roslyn-batch-output.json
          python -c "import json; payload=json.load(open('roslyn-batch-output.json')); assert len(payload['files']) == 2"
