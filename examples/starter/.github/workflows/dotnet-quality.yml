name: .NET quality

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Run .NET Quality Enforcer
        id: quality
        uses: KaramTNC/dotnet-quality-enforcer@72c46cbcea893c530a98b84032f044a0064ba8eb # v0
        with:
          command: code-size
          arguments: --scope full
          policy-path: .quality/quality_policy.json

      - name: Print violations
        if: steps.quality.outputs.status == 'failed'
        run: echo '${{ steps.quality.outputs.violations }}'
